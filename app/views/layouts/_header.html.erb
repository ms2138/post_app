<nav class="navbar navbar-expand-lg navbar-dark" style="background-color: #0077cc;">
  <div class="container-fluid">
    <span class="navbar-brand mb-0 pb-0"><%= full_title(yield(:title)) %></span>
    <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto gap-2">
          <% if user_signed_in? %>
            <li class="nav-item">
              <%= link_to "New Post", new_user_post_path(current_user), class: "btn btn-outline-light" %>
            </li>
            <li class="nav-item">
              <%= link_to "New Feed", new_feed_path, class: "btn btn-outline-light" %>
            </li>
            <li class="nav-item">
              <%= link_to "Log Out", destroy_user_session_path, data: { "turbo-method": :delete }, class: "btn btn-outline-light" %>
            </li>
          <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, alt: 'Login', title: 'Login', class: "btn btn-outline-light" %>
          </li>
          <% end %>
        </ul> 
    </div>
  </div>
</nav>